<div class="background-primary">
  <div class="my-3 justify-content-start">
    <% if @packages.count == 1 %>
      <h2> <%= @packages.count %> result for <%= @search_country %> </h2>
    <% else %>
      <h2> <%= @packages.count %> results for <%= @search_country %> </h2>
    <% end %>
  </div>
  <div class="row">
    <% @packages.each do |package| %>
      <div class="card-container">
        <%= link_to package_path(package), class: 'text-decoration-none' do  %>
          <div class="card-package">
            <div class="photo-grid">
                <% package.items.first(4).each do |item| %>
                  <%= cl_image_tag(item.photo) %>
                <% end %>
            </div>
          <div class="justify-content-start py-2">
            <h3> <%= package.name %> </h3>
            <p>price: $<%= package.price %></p>
          </div>
        <% end %>
      </div>
      </div>
    <% end %>
  </div>
</div>

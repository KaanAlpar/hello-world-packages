<div class="container">
  <div class="row">
    <div class="col-sm-4">
      <h1><%= @item.name %></h1>
      <%= cl_image_tag @item.photo, height: 300, width: 400, crop: :fill %>
    </div>
  </div>
</div>
